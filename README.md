# cisco-ucs

Reusable Cisco UCS Manager automation: read-only inventory in `ansible/` (cisco.ucs collection) and `powershell/` (Cisco PowerTool). Lab-specific build suites live outside this repository. See `docs/` for the original per-repo READMEs (prerequisites, authentication, examples).

_Created 2026-09-06 by LabOps CLEAN-2 from the archived infra-automation repositories. Fresh Git history; the originals (with their full history) are preserved under `archive/` and in `~/_backups/`._
